# python
venv/
venv-doccano/
__pycache__/
*.pyc
.ipynb_checkpoints/

# raw data (huge, regeneratable from Kaggle)
data/raw/*
!data/raw/.gitkeep

# processed + labeled data: KEEP TRACKED (dashboard needs them on Streamlit Cloud)
# do not ignore data/processed or data/labeled

# model binaries (regeneratable, optional)
models/*.pkl

# env + secrets
.env
*.log
kaggle.json

# os
.DS_Store
Thumbs.db